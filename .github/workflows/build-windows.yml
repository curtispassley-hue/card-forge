name: Build CardForge4D 0.4 Windows EXE

on:
  workflow_dispatch:
  push:
    branches: [main]
    tags: ['v*']

permissions:
  contents: write

jobs:
  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Verify offline OCR runtime
        run: python -c "from rapidocr import RapidOCR; import onnxruntime; RapidOCR(); print('RapidOCR / ONNX Runtime ready')"

      - name: Smoke test
        run: |
          python -m tests.smoke_test
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          python -m unittest discover -v
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          python main.py --self-test source-test.json
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Build EXE
        run: pyinstaller --noconfirm --clean CardForge4D.spec

      - name: Test packaged EXE offline
        shell: pwsh
        run: |
          $report = Join-Path $PWD 'exe-test.json'
          $app = Start-Process dist\CardForge4D.exe -ArgumentList '--self-test', $report -PassThru -WindowStyle Hidden
          if (-not $app.WaitForExit(180000)) { $app.Kill(); throw 'EXE self-test timed out' }
          if ($app.ExitCode -ne 0) { Get-Content $report; throw 'EXE self-test failed' }
          $result = Get-Content $report -Raw | ConvertFrom-Json
          if (-not $result.passed) { throw 'EXE self-test did not pass' }
          Get-Content $report

      - name: Create distributable ZIP
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force release | Out-Null
          Copy-Item dist\CardForge4D.exe release\CardForge4D.exe
          Copy-Item README.md release\README.md
          Copy-Item LICENSE.txt release\LICENSE.txt
          Compress-Archive -Path release\* -DestinationPath CardForge4D-Windows.zip -Force
          git archive --format=zip --output=CardForge4D-Source.zip HEAD

      - name: Publish tested alpha download
        env:
          GH_TOKEN: ${{ github.token }}
        shell: pwsh
        run: |
          $tag = 'v0.4.1-alpha.${{ github.run_number }}'
          gh release view $tag 2>$null
          if ($LASTEXITCODE -ne 0) {
            gh release create $tag --target '${{ github.sha }}' --prerelease --title 'CardForge 4D 0.4.1 Alpha' --notes-file RELEASE_NOTES_0_4_1.md
            if ($LASTEXITCODE -ne 0) { throw 'Release creation failed' }
          }
          gh release upload $tag dist/CardForge4D.exe CardForge4D-Windows.zip CardForge4D-Source.zip exe-test.json --clobber
          if ($LASTEXITCODE -ne 0) { throw 'Release upload failed' }

      - name: Upload EXE artifact
        uses: actions/upload-artifact@v4
        with:
          name: CardForge4D-Windows
          path: |
            dist/CardForge4D.exe
            CardForge4D-Windows.zip
            CardForge4D-Source.zip
            exe-test.json
            source-test.json

      - name: Upload diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-diagnostics
          path: |
            *-test.json
            build/CardForge4D/warn-CardForge4D.txt
